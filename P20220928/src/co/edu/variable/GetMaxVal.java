//package co.edu.variable;
//
//public class GetMaxVal {
//	public static void main(String[] args) {
//		int[] randomAry = new int[5]; // 정수 5개를 담을크기만 선언. 
//		// 0~1사이의 수 이므로 0.3등이 나온다.  현재 변수가 정수선언이기때문에 최소 *10은 해줘야 정수값으로 나올수있음
//		randomAry[0] = (int) (Math.random() * 100);// 0 ~ 1 사이의 임의수 생성.
//		randomAry[1] = (int) (Math.random() * 100) + 1;
//		randomAry[2] = (int) (Math.random() * 100) + 1;
//		randomAry[3] = (int) (Math.random() * 100) + 1;
//		randomAry[4] = (int) (Math.random() * 100) + 1;
//		
////		int maxVal=0; // 배열오소 중 가장 큰값을 저장.
////		for(int i =0; i<5; i++) {
////			System.out.println(randomAry[i]);
////			if(randomAry[i] < maxVal) {
////				maxVal = randomAry[i];
////			}
////		}  
////		System.out.println("생성된 배열의 임의의 값중 최소값=> " + maxVal);
////	}
////}   
//}
//}
