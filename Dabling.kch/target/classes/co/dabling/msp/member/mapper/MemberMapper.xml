<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.micol.prj.book.mapper.BookMapper">
	<select id="memberSelectList" resultType="co.dabling.msp.members.vo.MemberVO">
		SELECT * FROM MEMBERS  ORDER BY MEMBER_CODE DESC
	</select>
	
	<select id="memberSelect" resultType="co.dabling.msp.members.vo.MemberVO">
		SELECT * FROM  MEMBERS
		WHERE MEMBER_CODE = #{memberCode}
	</select>
		
	<update id="memberUpdate" parameterType="co.dabling.msp.members.vo.MemberVO">
	 	UPDATE MEMBERS
	 	SET EMAIL = #{email},
	 		PASSWORD = #{password},
	 		NAME = #{name},
	 		PHONE = #{phone}
	 	WHERE MEMBER_CODE = #{memberCode}
	</update>
	
	<update id="dormantMember" parameterType="co.dabling.msp.members.vo.MemberVO">
	 	UPDATE MEMBERS
	 	SET  AUTHOR = 1
	 	WHERE MEMBER_CODE = #{memberCode}
	</update>
</mapper>