<%@page import="co.edu.board.BoardVO"%>
<%@page import="java.util.List"%>
<%@page import="co.edu.dao.BoardDAO"%>
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
   <%@taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c" %> 
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Expression Language(EL /) JSP Standard Tag Library(JSTL)</title>
</head>
<body>
	<%
		String info = (String) application.getAttribute("info");
		System.out.println(info);
		
		BoardDAO dao = new BoardDAO();
		List<BoardVO> list = (List<BoardVO>) dao.pageList(1);
		
		request.setAttribute("bList", list);
		
	%>
	<p>${info}</p> 
	<p>${"hong" eq "hoho" }
	<p>${55+22 }</p>
	<p>${2 > 1 }</p>
	<p>${param.page}</p>
	<p>app: ${sessionScope.mysession eq sessionScope.yoursession }</p>
	<p>app: ${sessionScope.mysession}</p>
	
	<c:set var="myName" value="홍길동"></c:set>
	<p>${myName }</p>
	<c:out value="${myName}"></c:out>
	<!-- 값을 담고싶으면 set -->
	
	
	
	
	<c:if test="${info eq 'hong' }">
		<p>같습니다</p>
	</c:if>

	<c:if test ="${!info2 }">
		<p>값이 없습니다</p>
	</c:if>
	<!-- if문 -->
	
	
	
	
	<c:set var="score" value="75"></c:set>  <!-- var는 변수이름 value는 값 -->
	
	<c:choose>
		<c:when test="${score >90 }">A</c:when>
		<c:when test="${score >80 }">B</c:when>
		<c:when test="${score >70 }">C</c:when>
		<c:otherwise><p>불합격</p></c:otherwise>   
	</c:choose>
	<!-- c:otherwise 는 else -->
	
	
	
	<c:forEach var="i" begin="1" end="10" step="1">
		<p>${i }</p>
	</c:forEach>
	<!-- forEach 반복문 변수 i, 시작값 1, 끝값 10, 1씩 올린다 -->
	
	
	
	<c:forEach var="board" items="${bList }">
		<p>글번호 ${board.boardNo }, 제목:${board.title }, 작성자:${board.writer }</p>
	</c:forEach>
	<!-- bList에 있는걸 board 라는 변수에 담겠다 p테그 안에는 보여줄 필드값 -->
	
	
</body>
</html>